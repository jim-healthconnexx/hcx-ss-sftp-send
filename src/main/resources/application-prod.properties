# HDC-24: Production profile — WARN logging, real AWS (no endpoint override)
# Security note: AWS_S3_ENDPOINT must NOT be set in production.
# The loggers actuator endpoint must be protected via network ACL or Spring Security.
logging.level.root=WARN

